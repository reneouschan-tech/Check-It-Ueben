services:
  - type: web
    name: check-it-trainer
    env: python
    plan: free
    buildCommand: python -m pip install -r requirements.txt
    startCommand: python server.py
    healthCheckPath: /
